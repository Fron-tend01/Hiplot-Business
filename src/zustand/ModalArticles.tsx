// import { create } from 'zustand';

// // Define el estado y el tipo de la tienda
// // Define el estado y el tipo de la tienda
// type StoreState = {
//   modalState: boolean;
//   setModalState: (modal: boolean) => void;
//   units: any; // Estado para el modal de unidades
//   setUnits: (units: any) => void;
// };

// // Modal de artículos
// export const storeModalArticles = create<StoreState>((set) => ({
//   modalState: false,
//   setModalState: (modal) => set({ modalState: modal }),
//   units: [], // Inicializar el estado para el modal de unidades
//   setUnits: (units) => set({ units: units }) // Aquí está la corrección
// }));
